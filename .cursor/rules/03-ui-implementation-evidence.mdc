---
description: UI 实现输出必须标注关键证据来源，避免 mcp-raw 与缓存事实重复或冲突
alwaysApply: true
---

# UI Evidence Contract（输出证据约束）

当任务涉及 Figma 还原并已进入业务 UI 实现阶段，回复必须包含“关键实现项 -> 来源文件”映射。

## 强制项
- 至少覆盖：布局结构、核心文案、主要颜色/字体 token、状态/交互判断。
- 每个实现项必须指定一个主来源（如 `raw.json`、`spec.md`、`state-map.md`、`mcp-raw-get-design-context.txt`）。
- 若使用 `mcp-raw`，需说明其用途是“补缺口/消歧”，不得与主来源重复描述同一事实。

## 冲突处理
- 当 `raw/spec` 与 `mcp-raw` 冲突时，先报告冲突点，再声明采用依据与原因。
- 未消除冲突前，不得声称“1:1 完整还原已完成”。

## 缺口声明
- 若 `raw.json.coverageSummary.missing` 含 `interactions/states/accessibility/flow/assets`，必须在交付说明中列出影响范围。
- 缺口未补齐时，明确标注“当前为受限还原”，避免误导验收。
