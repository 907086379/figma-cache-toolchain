---
description: 本地命令执行安全规则（PowerShell 7 优先，兼容 5.1）
alwaysApply: true
---

# Local Command Execution Safety（本地）

适用范围：本项目本地规则，防止 PowerShell 版本差异与编码问题导致脚本失败。默认按 PowerShell 7 执行，同时保留对 5.1 的兼容兜底。

## 1) 命令连接符（按环境）
- 在确认运行环境为 `pwsh`（7+）时，可使用 `&&` 或 `;`。
- 若命令可能落在 `Windows PowerShell 5.1`（旧终端/旧任务）执行，统一使用 `;`，并在关键步骤检查退出码。
- 需要跨环境稳定时，避免依赖仅某一版本支持的 shell 语法。

## 2) 脚本与 JSON 写入编码（强制）
- 写入 `.js/.json/.mdc` 时，统一使用 UTF-8 无 BOM。
- `Set-Content -Encoding utf8` 在 5.1 可能产出 BOM；在 7+ 默认无 BOM。
- 为保证一致性，优先长期安全写法：
  - `.NET`：`[System.IO.File]::WriteAllText(path, content, (New-Object System.Text.UTF8Encoding($false)))`

## 3) 命中故障后的处理（强制）
- 若因命令语法/转义/编码差异导致执行失败，必须：
  1. 使用安全替代写法修复并复跑命令；
  2. 在本地改造清单追加“故障 -> 修复 -> 验证结果”；
  3. 在最终回复中明确长期安全写法。

## 4) 最小验证（强制）
- 对脚本改动，至少执行一次直接命令验证（如 `node <script>` 或 npm script）。
- 对 JSON 改动，至少执行一次解析相关命令验证。