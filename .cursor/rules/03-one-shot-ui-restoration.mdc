---
description: 页面/组件实现时若信息充分，必须一次性交付 1:1 还原，禁止无必要分步返工
alwaysApply: true
---

# One-Shot UI Restoration（一次性交付）

当任务进入“实际生成页面/组件”的实现阶段时，若当前节点信息已足够完成 1:1 UI 还原，必须在同一轮内一次性完成，避免反复提醒与返工。

## 适用边界
- 仅在“业务 UI 实现阶段”生效，不改变 `01-figma-cache-core.mdc` 的数据层边界。
- 若仍处于缓存拉取、索引维护、validate 阶段，不触发本规则。

## 信息充分判定（满足即视为可一次性完成）
- 已有可用的结构与样式来源（如 `raw.json`、`spec.md`、`meta.json`、`state-map.md` 或等价上下文）。
- 关键布局、文本、颜色/变量、间距、状态与交互语义无关键缺口。
- 不存在阻断性前置条件（权限、缺资源、运行环境不可用等）。

## 强制执行
- 一次性完成页面/组件的主要还原项：结构、视觉样式、状态交互、基础响应式与可访问性。
- 同轮完成必要自检（至少含构建/类型/核心校验之一），再给出结论。
- 优先输出“可直接验收”的结果，禁止把可当轮完成的内容拆成多轮“先做一部分、下轮再补”。

## 明确禁止
- 在信息已充分时，使用“先搭骨架/先占位/先部分还原，后续再补齐”的默认策略。
- 无新增证据地重复索要同类信息，造成反复提醒和返工。

## 例外处理（仅限信息不足）
- 若确有缺口，必须一次性列出“缺失字段 + 影响范围 + 最小补充需求”。
- 在未补足前可暂缓实现，但不得宣称可 1:1 完成。
## 交付输出模板（强制）
当进入“一次性交付”路径并完成实现后，回复必须包含以下 4 项，且顺序固定：

1. 完成项：本轮一次性完成的页面/组件范围（结构、样式、状态、交互、响应式、可访问性）。
2. 验证项：本轮实际执行的校验命令与结果（构建/类型/核心测试至少一项）。
3. 未完成项：仅允许填写“无”或客观阻断项（禁止写可当轮完成却延后的事项）。
4. 缺口与请求：仅在信息不足时填写“缺失字段 + 影响范围 + 最小补充信息”；信息充分时写“无”。

## 输出约束
- 信息充分时，`未完成项` 与 `缺口与请求` 默认应为“无”。
- 禁止使用“下一轮再补”“后续再完善”作为常规收尾语。
- 如存在阻断，需明确阻断证据与最小解阻步骤，不得笼统表述。