---
description: 命令执行防回归：PowerShell/Node 参数与转义失败需自动沉淀到规则案例
alwaysApply: true
---

# Command Execution Anti-Regression（命令执行防回归）

当脚本或终端命令执行失败，且根因为 shell 参数、引号、转义、换行、编码或跨 shell 语法不兼容时，必须按本规则处理，避免重复踩坑。

## 触发条件
- 出现“命令本身逻辑正确，但在当前 shell 执行失败”的情况。
- 典型信号：PowerShell 报参数解析错误、引号闭合错误、`node -e` 脚本被截断或转义失真。

## 强制策略（PowerShell）
- 复杂脚本禁止默认使用 `node -e "..."`（尤其包含引号、正则、JSON、多行文本、反斜杠时）。
- 优先级从高到低：
  1. 写临时 `.js` 文件后执行 `node <temp-file>`。
  2. 使用 PowerShell here-string 组装脚本并通过 stdin 执行（`node -`）。
  3. 直接使用 PowerShell/.NET 文件 API 完成文本替换与写入。
- 若必须使用 `node -e`，仅限简单单行表达式，且执行后必须立即回读目标文件验证结果。

## 失败后自动沉淀（强制）
- 任何命中本规则的失败，在修复完成后必须把案例追加到本规则的“已知故障案例（持续追加）”。
- 记录格式固定为：`日期 | 场景 | 根因 | 失败写法 | 安全替代 | 验证结果`。
- 新案例不得覆盖旧案例；只能追加，确保可追溯。

## 已知故障案例（持续追加）
- 2026-04-15 | PowerShell 执行文本替换 | `node -e` 字符串在传参时被 PowerShell 转义破坏 | `node -e "...复杂替换逻辑..."` | 改为 here-string + `node -` 或写临时 `.js` 文件执行 | 替换脚本可稳定运行且目标文件回读正确

## 输出要求
- 当本轮触发过此类失败，最终回复需明确：
  - 是否已按本规则完成替代执行；
  - 是否已追加案例记录；
  - 当前采用的长期安全写法。